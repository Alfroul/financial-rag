FACT_EXTRACTION_PROMPT = """你是一个金融知识提取专家。请从以下文本中提取结构化的知识条目。

## 要求
1. 每个知识条目必须包含一个明确的主题词（topic）和一个完整的事实描述（fact）
2. 主题词应为名词短语，不超过10个字
3. 事实描述应为50-150字的完整陈述，包含具体数字和来源
4. 为每个条目分配分类标签，可选：宏观经济、个股分析、财务指标、行业分析、政策法规、市场数据
5. 为每个条目提取实体关系三元组（triples），每个三元组包含 head（主体实体）、relation（关系）、tail（客体实体/值）
6. 关系类型限于：属于、包含、同比增长、环比增长、发布、对比、影响、高于、低于、等于
7. 如果文本中没有有价值的金融知识，返回空列表

## 输出格式
请以JSON数组格式输出，每个元素包含以下字段：
- topic: 主题词（字符串）
- fact: 事实描述（字符串）
- category: 分类标签（字符串数组）
- triples: 实体关系三元组（对象数组，每个包含 head、relation、tail 字段）

## 示例
输入文本："贵州茅台2024年营收1680亿元，同比增长15.6%，净利润862亿元。"
输出：
[
  {
    "topic": "贵州茅台营收",
    "fact": "贵州茅台2024年实现营收1680亿元，同比增长15.6%，净利润862亿元，继续保持白酒行业龙头地位。",
    "category": ["个股分析", "财务指标"],
    "triples": [
      {"head": "贵州茅台营收", "relation": "同比增长", "tail": "15.6%"}
    ]
  }
]

## 待提取文本
{context}

## 输出（仅JSON数组，不要其他内容）"""
